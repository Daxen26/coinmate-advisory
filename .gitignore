# ── SECRETS — NEVER COMMIT ──────────────────────────────
okx-keys.json

# local data & generated files
Ledger/
.claude/settings.local.json
node_modules/
.wrangler/
*.zip
.DS_Store
Thumbs.db

# cloud deploy: public/ is generated from the app at deploy time (one source of truth)
cloud/public/
